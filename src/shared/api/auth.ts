// Auth API
// Note: getProfile API removed - endpoint returns 500 error and is not used

// import api from './http';
// import type { ProfileDto } from '../types/auth';

// Removed getProfile API - not used and causing 500 errors
// export const getProfile = () => api.get<ProfileDto>('/api/auth/profile');
